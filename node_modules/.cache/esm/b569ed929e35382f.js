let Joi,bcrypt,User,CustomErrorHandler;_9e3‍.x([["default",()=>_9e3‍.o]]);_9e3‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_9e3‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_9e3‍.w("../../Models",[["User",["User"],function(v){User=v}]]);_9e3‍.w("../../service/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);




const loginController = {
  async login(req, res, next) {
    // Validation
    const loginSchema = Joi.object({
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp(/.{3,30}/))
        .required(),
    });
    const { error } = loginSchema.validate(req.body);

    if (error) {
      return next(error);
    }

    try {
      const user = await User.findOne({ email: req.body.email });
      if (!user) {
        return next(CustomErrorHandler.wrongCredentials());
      }
      // compare the password
      const match = await bcrypt.compare(req.body.password, user.password);
      if (!match) {
        return next(CustomErrorHandler.wrongCredentials());
      }
      res.json(user);
    } catch (err) {
      return next(err);
    }
  },
  async me(req, res, next) {
    try {
      const user = await User.findById(req.body._id); // req.user contains the decoded token
      if (!user) {
        return next(CustomErrorHandler.notFound('User not found'));
      }
      res.json(user);
    } catch (err) {
      return next(err);
    }
  },
};

_9e3‍.d(loginController);
